spring.application.name=user-service

# Database - sử dụng biến môi trường hoặc default
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/user_db?useUnicode=true&characterEncoding=UTF-8}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:userservice}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:user}
spring.jpa.hibernate.ddl-auto=update

server.port=8181

# JWT - sử dụng biến môi trường hoặc default
jwt.secretKey=${JWT_SECRET_KEY:5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437}
jwt.expiration=${JWT_EXPIRATION:86400000}

# OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Notification service
notification.service.url=${NOTIFICATION_SERVICE_URL:http://localhost:8084/api/v1/notifications}
app.frontend.url=${APP_FRONTEND_URL:http://localhost:4300}

# File upload
file.upload-dir=${FILE_UPLOAD_DIR:src/main/resources/uploads}
app.base-url=${APP_BASE_URL:http://localhost:8181}

# Upload size limit
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Eureka Client
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true


# spring.application.name=user-service
# spring.datasource.url=jdbc:mysql://localhost:3306/user_db?createDatabaseIfNotExist=true
# spring.datasource.username=root
# spring.datasource.password=1999
# spring.jpa.hibernate.ddl-auto=update
# server.port=8081

# #jwt
# jwt.secretKey=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
# jwt.expiration=86400000

# #openapi
# springdoc.api-docs.path=/api-docs
# springdoc.swagger-ui.path=/swagger-ui.html

# notification.service.url=http://localhost:8084/api/v1/notifications
# app.frontend.url=http://localhost:4200

# file.upload-dir=src/main/resources/uploads
# app.base-url=http://localhost:8081


# spring.servlet.multipart.max-file-size=10MB
# spring.servlet.multipart.max-request-size=10MB

# #create database
# spring.datasource.url=jdbc:mysql://localhost:3306/user_service_db
# spring.datasource.username=userservice
# spring.datasource.password=user

# ==============================================================
# CẤU HÌNH MONITORING (ACTUATOR + PROMETHEUS)
# ==============================================================
# 1. Mở các endpoints cần thiết (Health check và Prometheus)
management.endpoints.web.exposure.include=health,info,prometheus

# 2. Bật tính năng thu thập metrics cho Prometheus
management.endpoint.prometheus.enabled=true

# 3. Gắn nhãn tên App vào từng metric (Để lên Grafana biết là của order-service)
management.metrics.tags.application=${spring.application.name}

# 4. (Tùy chọn) Hiển thị chi tiết health check
management.endpoint.health.show-details=always

management.metrics.distribution.percentiles-histogram.http.server.requests=true
