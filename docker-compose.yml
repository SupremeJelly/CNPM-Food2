services:
  mysql-db:
    image: mysql:8.0
    container_name: mysql-db
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_USER: appuser
      MYSQL_PASSWORD: apassword
    command: ["--character-set-server=utf8mb4", "--collation-server=utf8mb4_unicode_ci"]
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      # optional: put init SQL files (create DBs/users) in ./mysql-init
      - ./mysql-init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD","mysqladmin","ping","-h","localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - cnpm-net

  payment-service:
    build:
      context: ./payment-service
      dockerfile: Dockerfile
    container_name: payment-service
    environment:
      SPRING_DATASOURCE_URL: "jdbc:mysql://mysql-db:3306/payment_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8"
      SPRING_DATASOURCE_USERNAME: "paymentservice"
      SPRING_DATASOURCE_PASSWORD: "pay"
      # Service URLs for Feign Clients
      ORDER_SERVICE_URL: "http://order-service:8084"
      RESTAURANT_SERVICE_URL: "http://restaurant-service:8083"
    ports:
      - "8086:8086"
    depends_on:
      mysql-db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - cnpm-net

  order-service:
    build:
      context: ./order-service
      dockerfile: Dockerfile
    container_name: order-service
    environment:
      SPRING_DATASOURCE_URL: "jdbc:mysql://mysql-db:3306/order_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8"
      SPRING_DATASOURCE_USERNAME: "orderservice"
      SPRING_DATASOURCE_PASSWORD: "order"
      # Service URLs for Feign Clients
      USER_SERVICE_URL: "http://user-service:8082"
      RESTAURANT_SERVICE_URL: "http://restaurant-service:8083"
      NOTIFICATION_SERVICE_URL: "http://notification-service:8084/api/v1/notifications"
    ports:
      - "8084:8084"
    depends_on:
      mysql-db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - cnpm-net

  restaurant-service:
    build:
      context: ./restaurant-service
      dockerfile: Dockerfile
    container_name: restaurant-service
    environment:
      SPRING_DATASOURCE_URL: "jdbc:mysql://mysql-db:3306/restaurant_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8"
      SPRING_DATASOURCE_USERNAME: "restaurantservice"
      SPRING_DATASOURCE_PASSWORD: "res"
    ports:
      - "8083:8083"
    depends_on:
      mysql-db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - cnpm-net

  user-service:
    build:
      context: ./user-service
      dockerfile: Dockerfile
    container_name: user-service
    environment:
      SPRING_DATASOURCE_URL: "jdbc:mysql://mysql-db:3306/user_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8"
      SPRING_DATASOURCE_USERNAME: "userservice"
      SPRING_DATASOURCE_PASSWORD: "user"
      # JWT Configuration - QUAN TRỌNG!
      JWT_SECRET_KEY: "5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437"
      JWT_EXPIRATION: "86400000"
      # File upload
      FILE_UPLOAD_DIR: "/app/uploads"
      APP_BASE_URL: "http://user-service:8082"
      # Frontend URL
      APP_FRONTEND_URL: "http://localhost:4300"
      # Notification service URL
      NOTIFICATION_SERVICE_URL: "http://notification-service:8084/api/v1/notifications"
    ports:
      - "8082:8082"
    depends_on:
      mysql-db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - cnpm-net
    volumes:
      - user-uploads:/app/uploads

  api-gateway:
    build:
      context: ./api-gateway
      dockerfile: Dockerfile
    container_name: api-gateway
    environment:
      # Service URLs - sử dụng container names
      RESTAURANT_SERVICE_URL: "http://restaurant-service:8083"
      ORDER_SERVICE_URL: "http://order-service:8084"
      USER_SERVICE_URL: "http://user-service:8082"
      PAYMENT_SERVICE_URL: "http://payment-service:8086"
    ports:
      - "9001:9001"
    depends_on:
      - mysql-db
      - order-service
      - restaurant-service
      - user-service
      - payment-service
    networks:
      - cnpm-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - "4300:4300"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - api-gateway
    networks:
      - cnpm-net

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    networks:
      - cnpm-net
    restart: unless-stopped

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana-data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning
    depends_on:
      - prometheus
    networks:
      - cnpm-net
    restart: unless-stopped

volumes:
  mysql-data:
  user-uploads:
  prometheus-data:
  grafana-data:

networks:
  cnpm-net:
    driver: bridge


# services:
#   zookeeper:
#     image: confluentinc/cp-zookeeper:6.2.10
#     environment:
#       ZOOKEEPER_CLIENT_PORT: 2181
#       ZOOKEEPER_TICK_TIME: 2000
#     ports:
#       - 22181:2181

#   kafka:
#     image: confluentinc/cp-kafka:6.2.10
#     depends_on:
#       - zookeeper
#     ports:
#       - 29092:29092
#     environment:
#       KAFKA_BROKER_ID: 1
#       KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
#       KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:29092
#       KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
#       KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
#       KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

#   eureka-server:
#     build: ./eureka-server
#     ports:
#       - 8761:8761

#   api-gateway:
#     build: ./api-gateway
#     ports:
#       - 8080:8080
#     depends_on:
#       - eureka-server

#   user-service:
#     build: ./user-service
#     ports:
#       - 8081:8081
#     depends_on:
#       - eureka-server
#       - mysql

#   restaurant-service:
#     build: ./restaurant-service
#     ports:
#       - 8082:8082
#     depends_on:
#       - eureka-server
#       - mysql

#   order-service:
#     build: ./order-service
#     ports:
#       - 8083:8083
#     depends_on:
#       - eureka-server
#       - mysql

#   frontend:
#     build: ./frontend
#     ports:
#       - 4200:4200
#     depends_on:
#       - api-gateway

#   mysql:
#     image: mysql:8.0
#     environment:
#       MYSQL_ROOT_PASSWORD: root
#       MYSQL_DATABASE: food_order_db
#     ports:
#       - 3306:3306