apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-service
  namespace: cnpm-food2
  labels:
    app: restaurant-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: restaurant-service
  template:
    metadata:
      labels:
        app: restaurant-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8182"
    spec:
      initContainers:
      - name: wait-for-mysql
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z mysql 3306; do echo waiting for mysql; sleep 2; done']
      containers:
      - name: restaurant-service
        image: onlykohi/cnpm-restaurant-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8182
          name: http
        env:
        - name: JAVA_OPTS
          value: "-Xms256m -Xmx384m -XX:+UseG1GC -XX:MaxGCPauseMillis=100"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:mysql://mysql:3306/restaurant_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
        - name: SPRING_DATASOURCE_USERNAME
          value: "restaurantservice"
        - name: SPRING_DATASOURCE_PASSWORD
          value: "res"
        - name: UPLOAD_DIR
          value: "/app/uploads"
        startupProbe:
          httpGet:
            path: /actuator/health
              port: 8182
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 20
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8182
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8182
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: restaurant-service
  namespace: cnpm-food2
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/actuator/prometheus"
    prometheus.io/port: "8182"
  labels:
    app: restaurant-service
spec:
  type: ClusterIP
  ports:
  - port: 8182
    targetPort: 8182
    protocol: TCP
    name: http
  selector:
    app: restaurant-service
