apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: cnpm-food
  labels:
    app: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8183"
    spec:
      initContainers:
      - name: wait-for-mysql
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z mysql 3306; do echo waiting for mysql; sleep 2; done']
      containers:
      - name: order-service
        image: onlykohi/cnpm-order-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8183
          name: http
        env:
        - name: JAVA_OPTS
          value: "-Xms256m -Xmx384m -XX:+UseG1GC -XX:MaxGCPauseMillis=100"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:mysql://mysql:3306/order_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
        - name: SPRING_DATASOURCE_USERNAME
          value: "orderservice"
        - name: SPRING_DATASOURCE_PASSWORD
          value: "order"
        - name: RESTAURANT_SERVICE_URL
          value: "http://restaurant-service:8182"
        - name: PAYMENT_SERVICE_URL
          value: "http://payment-service:8184"
        - name: USER_SERVICE_URL
          value: "http://user-service:8181"
        startupProbe:
          httpGet:
            path: /actuator/health
            port: 8183
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 20
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8183
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8183
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: cnpm-food
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/actuator/prometheus"
    prometheus.io/port: "8183"
  labels:
    app: order-service
spec:
  type: ClusterIP
  ports:
  - port: 8183
    targetPort: 8183
    protocol: TCP
    name: http
  selector:
    app: order-service
